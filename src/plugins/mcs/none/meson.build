mcs_none = shared_module(
    'mcs_none', # executable name
    'mcs_none.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)