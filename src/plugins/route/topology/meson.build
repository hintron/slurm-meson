route_topology = shared_module(
    'route_topology', # name
    'route_topology.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)