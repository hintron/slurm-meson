route_default = shared_module(
    'route_default', # name
    'route_default.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)