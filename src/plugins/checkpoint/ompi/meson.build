checkpoint_ompi_plugin = shared_module(
    'checkpoint_ompi', # name
    'checkpoint_ompi.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)