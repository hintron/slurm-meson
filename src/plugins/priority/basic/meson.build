priority_basic_plugin = shared_module(
    'priority_basic', # executable name
    'priority_basic.c', # source files
    include_directories: [slurm_inc],
    install : true,
    install_dir: pkglibdir,
    name_prefix: '',
)