proctrack_pgid = shared_module(
    'proctrack_pgid', # name
    'proctrack_pgid.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)