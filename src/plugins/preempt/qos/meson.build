preempt_qos_plugin = shared_module(
    'preempt_qos', # name
    'preempt_qos.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)