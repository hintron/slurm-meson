preempt_partition_prio_plugin = shared_module(
    'preempt_partition_prio', # name
    'preempt_partition_prio.c', # source files
    include_directories: [slurm_inc],
    install: true,
    install_dir: pkglibdir,
    name_prefix: '',
)